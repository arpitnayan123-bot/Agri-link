# AgriLink Bharat — Worklog

---

Task ID: 1, 2, 3, 4 (core build, executed by main agent)
Agent: Z.ai Code (main)
Task: Build "AgriLink Bharat" MVP prototype — B2B2C agri supply-chain platform (TPO crops, Maharashtra & Punjab pilot) per user spec, adapted to sandbox stack (Next.js 16 + TypeScript + Prisma/SQLite + shadcn/ui + socket.io mini-service).

Work Log:
- Designed full Prisma schema (13 models): User, Farm, Listing, QualityReport, PriceQuote, Order, OrderItem, OrderEvent, Shipment, ShipmentCheckpoint, Payment, MandiPrice, Notification. Pushed via `bun run db:push`.
- Built deterministic seed engine (src/lib/seed.ts): 2 FPOs + admins, 9 farmers (MH/PB, mixed KYC), 6 buyers, 3 logistics partners, 13 crop lots, 90-day mandi price series ×4 series, open buyer quotes, 6 orders across the full lifecycle (PLACED→SETTLED) with shipments/checkpoints/payments, SMS/WhatsApp outbox. Auto-seeds lazily via `ensureSeeded()`.
- Implemented 15 API routes under src/app/api/*: health, seed, users, listings(+[id]), quotes, orders(+[id], +[id]/advance state machine), shipments(+[id]/ping GPS+telemetry sim, +[id]/accept), quality (mock AI vision grading), prices (series+heuristic 7-day forecast), notifications, analytics (KPIs incl. loss-reduction model), kyc, advisor (z-ai LLM negotiation advice with heuristic fallback). Demo-auth via x-demo-user header (documented; prod = OIDC/Auth0).
- Built single-page frontend (src/app/page.tsx) with 4 role dashboards: Farmer (vernacular EN/HI/MR/PA i18n, low-data+escrow trust banners, price intelligence chart, List Harvest dialog with KYC gate, AI grade action, SMS notifications), Buyer (filterable lot marketplace with grade/mandi comparison, Create Order + Quote dialogs, order book with detail sheet), Logistics (open loads with freight estimate, accept, active trips with live GPS/telemetry tracking panel, ping simulation, cold-chain breach alerts, return-leg opportunities), FPO/Admin (6 KPIs, GMV/pipeline/crop-mix charts, KYC verify queue, order monitor).
- Order detail sheet: 9-step lifecycle timeline, tracking tab (route progress, temp/humidity/GPS, checkpoints), escrow payment card, GST invoice card, AI quality certificate per line item.
- Realtime: socket.io mini-service (mini-services/realtime-service, socket.io :3003 via gateway + internal emit endpoint :3004). API mutations fan-out events (order:status, shipment:ping, quote:new, quality:graded, kyc:update); frontend useLiveEvents hook connects io('/?XTransformPort=3003'), toasts + invalidates queries; polling fallback when offline.
- Fixed during QA: (a) zustand setState-during-render in useIdentities → moved to useEffect; (b) logistics dashboard hid open loads (partnerId filter) → fetch all + client split; (c) shipments API excluded DELIVERED → return-legs section now populates; (d) KYC-pending farmer could open List Harvest → inline warning + submit disabled; (e) farmer default persona now prefers KYC VERIFIED (kycStatus desc).
- Browser QA via agent-browser (through gateway :81): created order ALB-ORD-10007 as buyer → advanced PLACED→CONFIRMED→ESCROW_HELD→DRIVER_ASSIGNED; accepted load as logistics partner; pinged shipment ALB-ORD-10005 to 100% → auto-DELIVERED with 5 checkpoints; AI-graded new lot (Grade A, 91%); verified KYC from admin; Hindi i18n switch verified; mobile 390px verified; socket status "Realtime connected". ESLint clean, 0 console errors.

Stage Summary:
- Working MVP: full TPO value chain flow (list → quote → order → escrow → logistics → quality → settlement) is browser-verified end-to-end.
- Key decisions: SQLite+Prisma (sandbox; Postgres path documented), mock AI grading heuristic + optional z-ai LLM advisor, Razorpay-test-mode escrow simulation, demo-auth header, socket.io mini-service with :3004 internal emit port (path '/' gateway quirk workaround).
- Artifacts: prisma/schema.prisma, src/lib/* (constants, i18n, types, seed, market, api, client, format), src/app/api/* (15 routes), src/components/agrilink/* (12 components), src/store/agrilink.ts, mini-services/realtime-service/.

---

Task ID: 6
Agent: (pending) docs subagent
Task: Production deliverables — tech justification, risk register, Postman collection, docker-compose, README, API docs, roadmap checklist.

---

Task ID: 6
Agent: docs subagent
Task: Write MVP documentation deliverables (7 files + 1 Dockerfile), grounded in the actual codebase (read worklog, prisma/schema.prisma, all 19 API route.ts files, constants, package.json, realtime-service, seed, api helpers, live-events).

Work Log:
- docs/TECH-JUSTIFICATION.md — monolith-first → strangler path (module→service mapping table incl. Kafka/Postgres/Redis/Elasticsearch on ECS/K8s), SQLite vs Postgres rationale, Node+TS vs FastAPI / AWS vs self-managed / Kong vs API-GW trade-offs, India-constraint design-response table (2G, hi/mr/pa, UPI, power).
- docs/RISK-REGISTER.md — 7 risks (R1 farmer smartphone/data → USSD/IVR/SMS; R2 middleman/FPO capacity; R3 quality trust & disputes (marks grading as mock); R4 payment fraud/escrow liquidity (Route vs nodal); R5 cold-chain (8°C limit, breach alerts); R6 DPDP Act 2023; R7 simulated price data over-trust) with Likelihood/Impact/Mitigation/Owner.
- docs/API.md — enumerated ALL routes by reading code: 19 route.ts files / 22 handlers (incl. GET /api stub, PATCH /api/notifications, GET+POST /api/quality, POST /api/listings). Documented envelope {ok,data}/{ok:false,error}, x-demo-user caveat (helper exists but routes trust body IDs), auto-seed via withSeed, per-endpoint params/response/curl, order-state side-effect table, realtime events table with payload examples (+ extras order:new, shipment:assigned found in code).
- postman/AgriLink-Bharat.postman_collection.json — v2.1, validated with JSON.parse: baseUrl=http://localhost:3000 var, Seed, Get Users (FARMER), and requests covering listings(/:id), quotes, orders(/:id, /:id/advance), shipments(/:id/accept, /:id/ping), quality, prices, analytics, notifications(+PATCH), kyc, advisor. Collection-level x-demo-user header via apikey auth + per-request headers; ID variables (farmerId etc.) documented as REPLACE_ME since seed IDs are generated cuids.
- docker-compose.dev.yml + mini-services/realtime-service/Dockerfile — node:22-alpine web (npm i → db:push → dev, :3000), no db service (comment explains SQLite file + ./db mount), realtime built from oven/bun:1 (EXPOSE 3003 3004). realtime uses network_mode: service:web because emitEvent hardcodes 127.0.0.1:3004; YAML validated (PyYAML). Comments note prod = RDS/ECS.
- README.md — what it is, MVP scope table, ASCII architecture, quickstart (bun + docker-compose), 7-step golden-path demo walkthrough per role, tech stack, real folder structure, doc links, known-limitations table (mock AI, simulated escrow/SMS/prices/telemetry, demo auth, SQLite).
- docs/NEXT-STEPS-CHECKLIST.md — 10 roadmap areas with checkboxes & acceptance criteria: real CV grading (TFLite/Vision training plan), Prophet on AgMarkNet, USSD/IVR/SMS, Razorpay Route/nodal (RBI PA/PG), MQTT cold-chain IoT, Tally/Zoho sync, Locust load test (500/20), OWASP ASVS L2 + OIDC, multi-state scaling/strangler, driver app.
- No existing source code modified.

Stage Summary:
- 8 files created: docs/{TECH-JUSTIFICATION,RISK-REGISTER,API,NEXT-STEPS-CHECKLIST}.md, postman/AgriLink-Bharat.postman_collection.json, docker-compose.dev.yml, mini-services/realtime-service/Dockerfile, README.md. Postman JSON + compose YAML machine-validated.
- Docs are honest-by-design: every simulated subsystem (AI grading heuristic, Razorpay test-mode escrow, SMS outbox, simulated AgMarkNet prices, demo auth header, ping-based telemetry) is explicitly labeled as mock in README/API/RISK-REGISTER, and no non-existent integrations are claimed.
- Discrepancies found vs original description (documented in API.md, not code-changed): worklog said "15 API routes" but there are 19 route files/22 handlers; GET /api/orders ignores the farmerId filter its comment claims; GET /api root is a non-enveloped stub; code also emits order:new & shipment:assigned; x-demo-user helper is not enforced by routes; seed has 9 farmers as a "representative sample" of the 500-farmer pilot.

---
Task ID: 7
Agent: Z.ai Code (main)
Task: Final verification, docs gap-fix, cron setup, handover.

Work Log:
- Implemented farmerId filter in GET /api/orders (gap found by docs subagent) and re-verified + lint clean.
- Agent-browser E2E (via gateway :81): golden paths verified — order create/advance, escrow card, load accept, GPS ping to delivery (auto-advance), AI grading, KYC verify, Hindi i18n, mobile 390px, socket.io "Realtime connected". 0 console errors, ESLint clean.
- Realtime mini-service running on :3003 (socket.io) / :3004 (internal emit); logs healthy.
- Created mandatory 15-min webDevReview cron job.

Stage Summary:
- Project complete for Phase-1 MVP prototype. All role dashboards, lifecycle state machine, escrow simulation, telemetry tracking, vernacular i18n, analytics, docs deliverables in /docs + README + Postman + docker-compose.dev.yml.
- Known limitations (documented): mock AI grading heuristic, simulated SMS outbox, demo-auth header, SQLite, emit endpoint hardcodes 127.0.0.1:3004.
- Next-phase suggestions: seed the SSE/WS shipment auto-ping loop, farmer order view tab, lot photo upload, price-quote expiry job.

---
Task ID: 3-b
Agent: docs subagent
Task: BhoomiDirect documentation deliverables

Work Log:
- Read context first: worklog.md (AgriLink → BhoomiDirect switch), prisma/schema.prisma (9 models), src/lib/{growth,payments,chain,bhoomi-seed,api-helpers}.ts, all 15 API route.ts files, package.json, README, docs/, postman/.
- docs/API.md (replaced old AgriLink content, same path) — full reference for all 17 handlers on 15 paths (root index, health, seed, farms GET+POST, farms/:id, advance, logs, mandi-prices, orders GET+POST, orders/:id, orders/:id/advance, skills, skills/watch, farmer/dashboard, stats): envelope {ok:true,data}/{ok:false,error}, auto-seed via withSeed, demo auth model (no auth; farmer actions trust farmerId, consumer = buyerPhone +91XXXXXXXXXX), order lifecycle PAID_TO_FARMER→LOGISTICS_ASSIGNED→OUT_FOR_DELIVERY→DELIVERED with per-transition side effects, exact payment math (goods +15% platform +1.5% UPI on goods + max(35, kg×2.5, mango 5) −5% cashback; farmerPayoutRs = goodsRs instant; verified numerically with worked examples), BhoomiChain hash = 0x + sha256(prevHash|farmId|activity|note|ISO-at), GENESIS 0x+40 zeros, NDVI stage bands, curl per endpoint, error reference, mock register.
- docs/TECH-JUSTIFICATION.md — Next.js 16 all-in-one vs React+Fastify honest trade-off table (unified deploy, RSC, sandbox single-process constraint; API layer framework-agnostic: pure libs in src/lib, ~1-day Fastify port; Node event loop fine for concurrent UPI callbacks), SQLite→Postgres/Aurora triggers, SVG+CSS <200KB vs Three.js/Lottie for 2G, mock Razorpay X semantics + production Razorpay Route/X + RBI PA/PG + nodal-vs-instant-payout trade-off, India-first constraints→design-response table, 8-subsystem mock table (Bhuvan NDVI, Razorpay X, Shadowfax, Agmarknet, Aadhaar KYC, WhatsApp OTP, KVK feed, BhoomiChain→Polygon).
- docs/RISK-REGISTER.md — top-3 summary (R3 payout fraud, R2 fake profiles, R7 demand concentration) + 7-row register R1–R7 with Likelihood/Impact/Mitigation/Owner exactly as briefed (literacy→FPO/KVK assisted onboarding+voice notes+vernacular; misrepresentation→Aadhaar+liveness mock, KVK badge, chain logs, spot audits; fraud→PA/PG, payout caps, device fingerprinting; NDVI accuracy→farmer photo gate + KVK checks; cold-chain→SOP/SLA/seasonal rollout; DPDP 2023→hash-only, response trimming; concentration→waitlist/FPO aggregation/staged states). Review cadence + reopen triggers.
- docs/SKILL_BUILDING_GUIDE.md — Learn&Earn loop as coded (GET /api/skills → POST /api/skills/watch {skillId,buyerRef,unitPrice} → badge + per-unit 5% quote; checkout applies 5% of goodsRs via skillWatched flag; platform-subsidized, farmer price untouched); KVK background (ICAR network, ~730 district KVKs), approach via ATARI zonal MoU + SAMETI, 3-KVK pilot matching seeded providers; co-creation workflow (script → farmer-demonstrator video → dual review → JSON card); exact Skill-model JSON schema table (crop/title/titleHi/summary/summaryHi/videoUrl/durationMin/badge/cashbackPct/provider/lang, 5 allowed badges), 2 sample cards (mr, pa), governance (two-key review, immutable versioning, vernacular QA, attribution, badge-integrity rule).
- docs/NEXT-STEPS-CHECKLIST.md — checkboxes grouped (a)–(j): Bhuvan/VEDAS NDVI, Razorpay Route/X + PA/PG, Shadowfax/Delhivery rural API + webhooks, Aadhaar OTP + liveness, WhatsApp OTP, Lottie pipeline from farmer time-lapses, ONDC NP, USSD/IVR fallback, Postgres/Aurora+CDN+state scale-out, impact measurement + third-party audit; each with acceptance criteria tied to existing code seams.
- postman/BhoomiDirect.postman_collection.json — v2.1, name "BhoomiDirect API", baseUrl=http://localhost:3000, folders Setup/Farms/Orders/Skills/Farmer/Stats, 15 requests (Health, Seed, List Farms w/ crop+state+organic query examples, Farm Detail, Advance Farm 48h, Add Farm Log, Mandi Prices, Get Skills, Watch Skill, Create Order with raw-JSON body + payout.id/utr documented, List Orders, Get Order, Advance Order, Farmer Dashboard, Stats); collection vars farmId/listingId/orderId/skillId/farmerId/buyerPhone; descriptions tell readers to run List Farms / List Skills first (seeded ids are cuids). VALIDATED: python3 json.load parses; folders/requests/vars asserted.
- README.md — replaced AgriLink README: what BhoomiDirect is, trust math (₹100/kg × 10kg → ₹1,190 paid, farmer ₹1,000 ≈ 84%, ~88% with cashback, vs 22–35% mandi; honest footnote that the ₹35 logistics minimum compresses tiny baskets), quickstart (bun install / db:push / dev / auto-seed), MVP scope, ASCII architecture, folder tree, key-endpoints table, deliverable links, 10-row mocked-integrations table, tech stack.
- Appended only; no source under src/ or prisma/ touched (git status: docs/README/postman only).

Stage Summary:
- 7 deliverables written/updated: docs/{API,TECH-JUSTIFICATION,RISK-REGISTER,SKILL_BUILDING_GUIDE,NEXT-STEPS-CHECKLIST}.md, postman/BhoomiDirect.postman_collection.json (JSON parse-validated), README.md. All grounded in the committed backend; every mock explicitly labeled.
- Discrepancies found (documented, code untouched): (1) brief said "16 endpoints" — code has 17 handlers on 15 paths (GET+POST on /api/farms and /api/orders, 405 GET guard on /api/seed); (2) POST /api/farms writes voiceNote: into Farm.create but the model field is voiceNoteUrl → Prisma validation error at runtime (flagged in API.md as known bug); (3) orderCode() yields BD-ORD-0001-style codes, not the BD-ORD-10001 the schema comment claims; (4) GET /api/skills ignores the lang param its descriptor advertises; (5) GET /api/farms/:id returns the FULL farmer object incl. phone/upiId/aadhaarHash (privacy trim needed pre-prod, R6); (6) mockRazorpayXPayout ignores its inputs; (7) skills/watch cashback quote is per-unit while checkout applies 5% of full goodsRs (nuance documented); (8) GENESIS_HASH is 0x+40 zeros vs 0x+64-hex regular hashes; (9) trust-math "~84–88%" holds only at ~10kg+ baskets (₹35 logistics minimum) — README/API state this honestly; (10) frontend is still the old AgriLink page.tsx/layout.tsx importing deleted modules (@/components/agrilink/*, @/store/agrilink, @/lib/i18n, @/lib/client, @/lib/types) → the app won't compile until the living-haat UI lands; README "Current state" makes the API the working surface (Postman tour).
- Leftover old-product artifacts intentionally untouched (not in scope): postman/AgriLink-Bharat.postman_collection.json, docker-compose.dev.yml, mini-services/realtime-service/, Caddyfile, examples/websocket/.

---
Task ID: 1, 2, 3-a, 4, 5, 6
Agent: Z.ai Code (main)
Task: Build "BhoomiDirect" — live-animated, zero-middleman farm-to-consumer platform (replaces AgriLink Bharat as the / app), per user's production-grade prompt: living field animations, instant UPI payout to farmers, Learn & Earn KVK skills, transparency/BhoomiChain trust layer, EN/HI vernacular + low-data mode, full doc deliverables, git commits at every milestone.

Work Log:
- [1] Assessed workspace: AgriLink Bharat MVP existed (git-committed b5df782). New product is consumer-facing & animation-first → full replacement planned (old code recoverable via git history).
- [2] New Prisma schema (9 models: Farmer, Farm, Listing, ChainLog, Order, OrderEvent, Skill, SkillWatch, MandiPrice), db push --force-reset. Seed: 5 verified farmers (Sunita/Ramesh/Gurpreet/Balvir/Dattatray — MH+PB), 5 farms with coherent plantedAt/NDVI/stage per crop maturity curves, hash-chained field logs (sha256 prevHash-chain, GENESIS 0x0…0), 8 KVK skill cards, 9 mandi price rows, 2 demo orders (₹300 total instant-paid to farmers).
- [3-a] 17 handlers / 15 paths under src/app/api: farms (list/detail/plant), advance (48h ISRO-Bhuvan-mock NDVI→stage engine + chain anchor), logs (chain append), orders (validate→transparent split→mock consumer UPI→INSTANT Razorpay-X-mock farmer payout→Shadowfax-mock logistics assign→stock decrement→farmer earnings increment), orders/:id/advance lifecycle, skills + watch (badge + 5% cashback), farmer/dashboard (lifetime earnings, uplift vs mandi, farmer-share-of-consumer-₹), mandi-prices, stats, seed, health. Libs: growth.ts (NDVI bands/curves), payments.ts (fee math: farmer price never touched; platform 15% + UPI 1.5% + logistics actual on top; ₹35 logistics min documented honestly), chain.ts (hash/verify).
- [3-b] Docs subagent delivered: docs/API.md, TECH-JUSTIFICATION.md, RISK-REGISTER.md (7 risks), SKILL_BUILDING_GUIDE.md (KVK partnership playbook + card JSON schema), NEXT-STEPS-CHECKLIST.md (a–j roadmap), postman/BhoomiDirect.postman_collection.json (v2.1, validated), README.md. It found 3 real bugs which I fixed: voiceNote→voiceNoteUrl in POST /api/farms; orderCode off-by-format; GET /api/farms/:id privacy trim (no aadhaarHash/phone/UPI to consumers, DPDP).
- [4] Frontend (src/components/bhoomi/*): field-animator.tsx — 5 crops × 6 stages pure SVG+CSS living scenes (sun w/ rotating rays, drifting clouds, swaying plants, unfurling leaves, blooming flowers, growing fruits incl. potato underground cutaway tubers, harvest basket, butterflies; low-data = static, no animation classes); india-map.tsx (stylized clickable India SVG w/ Punjab+MH hotspots + counts); farm-card, farm-detail-dialog (Live Field / Transparency [input-cost table, price ladder, BhoomiChain verified log viewer] / Your Farmer [raw-video placeholder + quotes] / Skills tabs + qty stepper + breakdown footer), buy-dialog (details→UPI processing→receipt w/ payout id+UTR), orders-view (payout-proof timeline + animated truck progress + demo advance), farmer-studio (identity switcher, earnings hero, plot grid w/ 48h advance + chain-log dialog + plant dialog), live-ticker (marquee), header/footer (role switch, EN/हिं toggle, low-data toggle, orders badge), EN/HI dictionary (~80 keys), zustand persist store, tanstack-query client.
- [5] QA via agent-browser: golden path E2E — farm detail→Truth tab (chain verified, hashes)→Pay via UPI (Anjali/Pune)→"Farmer has been paid!" receipt (payout_qcioeh4lwb0fsg, UTR)→orders timeline advanced to DELIVERED; Farmer Studio: ₹210 lifetime/+50% vs mandi ₹140, 48h advance NDVI 0.62→0.66, chain log "ड्रिप 40 मिनट" anchored; Learn&Earn badge toast (PEST WARRIOR); Hindi UI verified page-wide; low-data strips all animation classes (10 sway→0→10); mobile 390px no overflow, footer flush at bottom (gap math verified); ESLint clean; tsc clean; dev.log error-free; console clean post-fixes.
- Fixed during QA: farmerSharePct leaked into Prisma order.create (orders route + seed — spread bug, had silently skipped demo-order seeding), Turbopack stale-module reseed (server restart), SVG hydration mismatch (Node/Chrome float parity → 2dp rounding of ALL computed coords), radix DialogTitle missing on paying step, persisted-store hydration gate via useSyncExternalStore, hero band low-data respect, skill button i18n.
- Committed 3 milestones: 9b91f6b (data+API), 035b450 (frontend+docs), 513abc1 (QA fixes).

Stage Summary:
- BhoomiDirect MVP is browser-verified end-to-end: living animated marketplace → transparent direct purchase → instant farmer payout proof → tracked delivery; farmer studio with honest profit math; Learn & Earn loop; BhoomiChain tamper-evident ledger; EN/HI; low-data mode; mobile-safe.
- Key decisions: Next.js 16 full-stack (docs justify vs Fastify; API layer portable), SQLite (Postgres path documented), mocks clearly labelled (Bhuvan NDVI, Razorpay X, Shadowfax, Agmarknet, Aadhaar, WhatsApp OTP, KVK feed, Polygon anchor).
- Known limitations: farmer share 57-66% on ≤2kg baskets (₹35 logistics min — honest math, documented in README/API), voice notes are transcripts (no audio assets), single / route per sandbox rules.
- Next-phase suggestions: seed auto-advance loop for stages (cron), farmer photo uploads, real Lottie pipeline, Marathi/Punjabi UI packs, ONDC integration.

---
Task ID: 8
Agent: Z.ai Code (main)
Task: Round-8 — browser QA + wire orphaned backend features (watchlist/impact) into the frontend + badge shelf + visual polish.

Work Log:
- Assessed state: worklog showed commit c768b8f added backend APIs (GET /api/watchlist, GET+POST /api/farms/[id]/watch, GET /api/impact) and FarmWatch model, but ZERO frontend usage (grep confirmed). Gap = this round's main work.
- Browser QA (agent-browser, pre-change baseline): golden paths re-verified — buy flow (receipt "किसान को भुगतान हो गया!" payout_jejyn0dbckvo05 + UTR), orders view (BD-ORD-10001..10004), Farmer Studio (₹294 lifetime, +50% vs mandi, 73% share). Only console noise: 1 DialogContent a11y warning.
- Feature A (harvest watchlist): new WatchButton component (overlay bell on card image / outline in detail dialog) → POST /api/farms/:id/watch toggle with watcher count + toasts, react-query invalidation, store.watchedFarmIds persisted; farm-card restructured to layered layout (animation + bell + absolute cover button) to kill nested-<button> hydration errors; WatchButton also in farm-detail Field tab beside the 48h satellite button.
- Feature A2: WatchStrip "Growing for you" — GET /api/watchlist strip on market with mini FieldAnimator, stage chip, farmer+village, harvest countdown ("harvest day: 40 days" / "🧺 Ready now — buy fresh"); hidden until ≥1 watch; horizontal scroll on mobile.
- Feature B: ImpactView "The transparent ledger" — GET /api/impact: 4 hero stats (orders, farmer share 69.1%, uplift +50%, 12 middlemen bypassed), farmer-vs-fees split bar, by-crop Bhoomi-vs-mandi dual bars with uplift badges, NABARD-2023 methodology note; entry via header 🧾 Impact button + hero CTA; store View extended to 'impact'.
- Badge shelf: 🎓 popover in header (5 badges EN/HI: SOIL_HEALTH_SCOUT/PEST_WARRIOR/FRESH_KEEPER/RIPENESS_PRO/WATER_WIZARD + earn-more hint), mounted-gated, hidden until first badge.
- i18n: ~38 new EN/HI keys (watchlist, impact, badges).
- Bugs fixed this round: (1) nested <button> in farm-card → layered layout; (2) float-precision "30.900000000000006%" in impact fee bar → rounded; (3) FieldAnimator in-scene 🌿 ORGANIC sign collided with new bell → showOrganicBadge prop (farm-card off, Organic stays as body pill); (4) farmer-studio Record-activity + Plant dialogs missing DialogDescription (a11y) → added, console now 100% clean.
- Verified via agent-browser: watch toggle round-trip (DB FarmWatch rows flip correctly, strip adds/removes farm), impact view renders all sections, badge popover, EN+HI, mobile 390px overflow 0, 0 console errors, 0 page errors, lint clean, src/ tsc clean.
- Committed: watchlist+impact+watch strip (feat), badge shelf + overlap fix (feat).

Stage Summary:
- All three orphaned backend features are now first-class UI: harvest watch bell (every farm card + detail), "Growing for you" countdown strip, and the Impact transparent ledger (header + hero entry). Learn&Earn badges finally visible in the header.
- Impact numbers live-computed from real orders (4 orders → 69.1% farmer share, +50% vs mandi, 12 middlemen bypassed); honest-fee note included.
- Console/ESLint/tsc: clean. Mobile-safe.
- Risks/notes: WatchStrip uses buyer.phone as buyerRef — 'guest' before first purchase is fine (isolated per browser); watcher count is a real DB count (demo-global, not per-buyer).
- Next-phase suggestions: WhatsApp/push notification simulation on stage change for watched farms (backend has the hook point in advance route), Impact view per-farmer drill-down, order-view share button (receipt as image), Marathi/Punjabi UI packs, low-data icon-only FieldAnimator variant tuning.

---
Task ID: 9 (rebrand v2)
Agent: Z.ai Code (main)
Task: Rebrand BhoomiDirect → AgriLink per user pivot: Blinkit/Zepto-grade simple quick-commerce UX, premium claymorphism+neumorphism design, AI-first differentiation, market research → architecture → build → browser QA.

Work Log:
- Market research (web-search): Blinkit ₹13,300Cr NOV/2027 dark stores/AOV ₹300-450 but sources from mandis (cold-stored, farmer share tiny); Zepto same middleman model; Ninjacart is B2B only; India organic market ₹9.58B→$24.39B @10.9% CAGR, consumers pay up to 20% premium for chemical-free. Winning formula locked: Blinkit UX + 100% farmer-direct instant payout + harvested-today freshness + AI layer.
- Logo: generated premium 3D claymorphism leaf+sun mark (public/agrilink-logo.png) via image-generation CLI; used as favicon + header/footer brand.
- Keep/Remove map: KEPT zero-middleman instant UPI payout w/ proof, chemical-free, Meet-Your-Farmer, EN/HI, 5 core crops; REMOVED farm-first NDVI listings, India map, skills/Learn&Earn UI, farmer studio, chain-log viewer, watchlist, notifications (all old bhoomi components/libs/routes deleted).
- New Prisma schema (4 models): Farmer(7, MH+PB w/ stories+UPI), Product(22, category/emoji/price/mrp/mandiPrice/harvestedAt/accent/tags), Order, OrderItem(payoutRef per farmer). db push --force-reset.
- Seed: 7 farmers × 22 products + 5 demo orders across statuses (DELIVERED/OUT_FOR_DELIVERY/PACKED) with per-farmer grouped payout refs → hero stats alive (7 farmers, 22 products, 5 orders, ₹1,225 paid, 87% farmer share, 20 kg).
- APIs: GET /api/products (category/q/recs-AI-picks), GET /api/stats, GET+POST /api/orders (stock check → mock UPI collect → INSTANT Razorpay-X-mock payout PER FARMER (split-payout) → stock decrement → payout proofs returned), POST /api/seed, POST /api/ai/chat (z-ai LLM grounded in live catalog w/ heuristic fallback), POST /api/ai/search (LLM keyword extraction → fallback substring), /api/health, /api index. Envelope simplified to raw data.
- Design system (globals.css): claymorphism (.clay + tinted variants clay-green/mango/tomato/sky/berry/lime w/ puffy dual shadows + inset highlights), neumorphism (.neu/.neu-inset/.neu-pressed), .al-add solid-green pill button, 11 keyframe animations (float/sway/pop/marquee/pulse/shimmer/truck/grow) + prefers-reduced-motion guard, warm-cream multi-blob body gradient, al-scroll/al-noscroll scrollbars. Font: Baloo 2 (display, latin+devanagari) + Geist.
- Components (src/components/agrilink/): header (logo, AI smart-search dropdown, lang toggle, orders, cart badge), ticker (live marquee w/ real stats+freshness), hero (framer-motion floats, count-up stats, promise chips), category-bar (neu chips), product-card (tinted clay hero, freshness ring, farmer line, strike-MRP, ADD/stepper), product-sheet (price-fairness bars mandi-vs-farmer-vs-supermarket, farmer story card, tags, sticky ADD bar), cart-bar (Blinkit floating bar "N farmers paid instantly"), cart-sheet (items → address form w/ validation → UPI paying animation → SUCCESS receipt w/ per-farmer payout proofs+UTR refs), orders-view (status timeline w/ animated progress + truck ETA + payout proof pills), ai-assistant (clay chat panel, quick prompts, typing dots, grounded LLM), freshness-ring (useSyncExternalStore 60s tick — lint-clean live decay), how-it-works + impact-strip (honest receipt 32% mandi vs 85% AgriLink bars), footer (sticky, safe-area).
- page.tsx: Market (hero→categories→grid→AI-picks strip when cart→how→impact) | Orders, all state in zustand persist (lang/cart/buyer/lastOrder).
- Browser QA (agent-browser): golden path E2E PASS — ADD×3 → cart bar "3 items · ₹131 · 3 farmers paid instantly" → address → pay → receipt "Farmers paid instantly!" w/ Sunita ₹32/Balvir ₹24/Ramesh ₹38 payout refs → Track order → timeline. AI assistant PASS ("Plan a ₹300 veggie basket" → grounded ₹215 basket w/ exact prices + zero-middleman line). Product sheet PASS (fairness bars ₹90/₹220/₹300, farmer story). Hindi PASS (full Devanagari in Baloo 2). Mobile 390px PASS (overflow 0, layout holds). 0 console errors, 0 page errors, ESLint clean.
- Fixed during build: lint react-hooks/set-state-in-effect in freshness-ring → useSyncExternalStore pattern; seed stale-module reseed → server restart; variable shadow in seed demo-orders → fetch db products.
- Committed 3 milestones: 3c1580c (schema+logo+cleanup), b34b57b (backend+seed), 4a90e3e (frontend+docs).

Stage Summary:
- AgriLink v2 live on / — quick-commerce simple, premium claymorphism, AI-native (freshness scores, assistant, smart search, picks), zero-middleman verified in UI.
- Key decisions: product-first schema w/ OrderItem.payoutRef for per-farmer split-payout proof; freshness computed from harvestedAt (decays live, server-stable via useSyncExternalStore); LLM grounded via catalog injection in system prompt; honest fee math (farmer price untouched, 8% platform + ₹29 delivery/free>₹249).
- Known limits: payments/AI mocks labeled in UI; demo phones seed orders; single / route (sandbox rule); old postman/docs reference BhoomiDirect APIs (docs refresh = next round).
- Next-phase suggestions: docs/postman refresh to AgriLink, order status auto-advance loop, per-farmer store page, real product imagery, dark mode.

---
Task ID: 10 (cron review round 1)
Agent: Z.ai Code (main)
Task: webDevReview — assess state, QA via agent-browser, fix bugs, add features + styling details.

Work Log:
- State check: worklog Task-9 handover reviewed; dev.log clean; /api/health + /api/stats healthy (7 farmers, 22 products, 5 orders, ₹1,225 paid, 87% share). Stale pre-rebuild console errors in old browser session — cleared, hard reload → 0 page errors, 0 console errors.
- Feature A — LIVE order lifecycle: schema +discountRs/promoCode on Order; GET /api/orders now auto-advances PAID→PACKED→OUT_FOR_DELIVERY→DELIVERED on a demo clock (STAGE_MS=150s, elapsed-since-createdAt, DB-persisted) and returns nextStageInMin countdown; OrdersView polls every 10s, shows pulsing "LIVE TRACKING" badge + "Live update in N min" chip. Verified: old seed orders auto-advanced to DELIVERED, fresh order stays PAID with countdown.
- Feature B — Farmer store sheets: FarmerSheet dialog (avatar hero, KYC badge, location, rating, "₹X earned directly · 100% theirs", story, their-products grid with ADD). Entry points: farmer name on every product card (dotted underline affordance) + "View store" button in product sheet (cross-sheet navigation). store.farmerSheetId added.
- Feature C — Promo: FRESH10 (10% off items, cap ₹100) client-validated + server-enforced (PROMOS map); discount platform-subsidized — farmerPayoutRs NEVER reduced (receipt shows farmer gets ₹282 when consumer paid ₹277 → the honest story in numbers). Free-delivery progress bar in cart ("Add ₹X more" / "🎉 FREE unlocked"). Bill line + success-receipt discount line. Invalid-code error handled server-side.
- Styling details (mandatory): product card + "Picked Xh ago" teal chip, ★rating, low-stock "Only N left" red pulse chip, keyboard-accessible cards (tabIndex + Enter/Space, focus-visible rings); sticky category bar under header (z-20, cream backdrop-blur); i18n +16 keys EN/HI (promo/urgency/farmer-store/live-tracking).
- Bug fixed: Runtime ReferenceError "t is not defined" in StatusTimeline (used t() without makeT) — caught via agent-browser on My Orders; fixed by computing makeT(lang) inside StatusTimeline; re-verified 0 errors.
- QA E2E: promo checkout golden path via browser — cart (FRESH10 −₹28, FREE delivery) → details → pay → receipt (payout proof ₹282, discount line) → Track order → LIVE TRACKING + countdown. Mobile 390px: overflow 0, sticky bar holds. ESLint clean after each file.
- Committed: round-10 commit (feat: live lifecycle + farmer sheets + promo + polish; fix: StatusTimeline).

Stage Summary:
- AgriLink now feels alive end-to-end: orders visibly progress through the lifecycle in real time; every product is a doorway to a farmer's store; promo economics prove the zero-middleman promise (discount never touches farmer payout).
- Known limits: CartBar total shows pre-promo estimate (promo lives in cart sheet — same as Blinkit); auto-advance is a demo clock (production = logistics webhooks); only promo FRESH10 registered.
- Next-phase suggestions: docs/postman refresh to AgriLink v2 APIs; per-farmer "follow" + WhatsApp notify simulation on stage change; AI basket one-tap "add all" from assistant replies; product sheet reviews mock; dark mode exploration.

---
Task ID: 11 (cron review round 2)
Agent: Z.ai Code (main)
Task: webDevReview — assess state, agent-browser QA, fix bugs, add features (AI add-all, follow+WhatsApp feed, reviews), styling details, docs/postman refresh.

Work Log:
- State check: worklog Task-10 reviewed; dev.log clean; health 7 farmers/22 products; stats 7 orders ₹2,387 paid. Git clean at e8c06a0.
- Browser QA (agent-browser, desktop+mobile): market/cart/checkout/receipt/orders/AI/farmer-sheet/Hindi/mobile-390px all pass; 0 console errors. Findings: (1) AI chat ignored budget — "fruits under 100" reply included ₹220 Alphonso Mango; (2) AI replies were plain text with no actionability; (3) metadataBase console warning; (4) pre-existing: 12 tsc errors in src/, stale README/docs/postman (BhoomiDirect-era), persisted buyer not applied to details form (zustand post-hydration), negative review dates (later).
- Feature A — AI basket v2 (/api/ai/chat): budget parser (₹X / under X / X mein / budget of X); system prompt requires machine line `PICKS: id1,...` (stripped from reply; name-match fallback); server-side budget enforcement drops over-budget picks and backfills cheapest in-budget items; returns { reply, picks[] (slim w/ emoji+price+farmer), source, budget }. Client: picks render as clay chips w/ per-item ADD (✓ when in cart) + "Add all to basket · ₹X" bar → store.add ×N + toast. Verified: "high vitamin C fruits under 100" → picks Pomegranate ₹95/Bananas ₹42/Guava ₹48 (mango flagged over budget); "veggies under 30" → server replaced all picks with ≤₹30 items. Add-all verified: cart bar "3 items · ₹229 · 1 farmer paid instantly".
- Feature B — Follow farmer: store.followedFarmerIds (persisted) + toggleFollow/isFollowing; pink Heart pill in farmer sheet (Following state) + toast (EN/HI).
- Feature C — WhatsApp-style live updates (OrdersView): 10s poll detects status transitions → toast "💬 AgriLink on WhatsApp: Order AL-10010 — Balvir packed your basket at the farm 🧬" + WhatsApp-styled feed card (green #075E54 header, beige chat bg, right-aligned #DCF8C6 bubbles w/ ✓✓ + time, honest "Demo simulation" footnote). Two real bugs found+fixed here: (a) lint set-state-in-effect workaround (setTimeout) was cancelled by cleanup before firing because `t` identity changed every render and the toast re-render re-ran the effect → moved announce+prev-advance INSIDE the timeout; (b) duplicate feed bubbles on a race → deterministic ids (`orderId-status`) + dedupe in updater. Verified live: PAID→PACKED→DELIVERED transitions each produced exactly one bubble.
- Feature D — Product sheet reviews: deterministic demoReviewsFor(productId, rating) in client lib (hash-picked EN/HI mixed comments, ±rating jitter, positive day offsets — fixed signed >> → >>> for "-6d" bug); rendered with "(Demo reviews)" label.
- Styling details (mandatory): product cards hover lift (-translate-y-1.5 + deeper shadow) + emoji rotate/scale on hover + ADD active:scale-90; cart bar press feedback; category chips show live product counts (All 22 / Bestsellers 6 / Veggies 13 / Leafy 3 / Herbs 2 / Fruits 4); footer payment trust chips (UPI Instant · Razorpay X payouts · BHIM · 100% secure).
- Bug fixes: buyer-form hydration (cart-sheet adopts persisted buyer when form untouched on entering details step — returning users no longer retype); reviews "-6d"; metadataBase in layout.tsx.
- Type hygiene: fixed ALL 12 pre-existing src/ tsc errors — orders route Prisma.OrderGetPayload + shared ORDER_INCLUDE; client Product += farmerId, Farmer += upiId (API-returned fields); cart-sheet removed phantom store.products destructure + PayResponse.discountRs non-optional; hero stat() value param ReactNode. tsc src/ now 0 errors, ESLint clean.
- Docs refresh: README.md rewritten for AgriLink v2 (trust math, section map, API table, architecture, mocked-integrations table); docs/API.md rewritten (9 handlers documented incl. AI chat PICKS protocol + budget parsing); new postman/AgriLink.postman_collection.json (11 requests, JSON-validated); old BhoomiDirect/AgriLink-Bharat collections left in place for history.
- Final QA: 0 page errors, 0 console errors, mobile 390px overflow 0, Hindi verified for all new strings (फ़ॉलो करें, feed, picks), lint clean, tsc src clean.
- Committed: 4dac80c (21 files, +776/−587).

Stage Summary:
- AgriLink round 11: the AI layer became actionable (budget-aware chat → one-tap basket), farmers became followable with WhatsApp-style delivery updates, products got social proof, and the whole repo's docs/type hygiene caught up to the code.
- Key decisions: PICKS machine-line protocol over function-calling (works with plain chat completions); budget enforced server-side (never trust the LLM); WhatsApp feed is client-side simulation over the real auto-advance poll (labeled).
- Known limits: feed/toasts are session-only (reset on reload — by design, real WhatsApp would be server-pushed); follows are local-only (no DB counter); reviews deterministic mocks; tool-results/ holds QA screenshots (untracked).
- Next-phase suggestions: server-persisted follow counts + real WhatsApp Business API hook point in orders GET advance; review submission UI; AI picks strip personalization memory; PWA manifest + add-to-home; date-fns for hi-IN relative times.

---
Task ID: 12 (cron review round 3)
Agent: Z.ai Code (main)
Task: webDevReview — assess state, agent-browser QA, fix bugs, add features (dark mode, real review system, PWA manifest), styling details.

Work Log:
- State check: worklog Task-11 reviewed; dev.log clean; health 7 farmers/22 products; stats healthy; lint/tsc clean. Baseline browser QA PASS: full checkout E2E (10-item cart → FRESH10 −₹51 → receipt w/ 6 per-farmer payout refs → LIVE TRACKING), mobile 390px overflow 0, 0 console errors → project stable → feature round.
- Feature A — DARK MODE (claymorphism-aware "forest night"): 30+ `--al-*` semantic tokens defined in :root + .dark (ink/green/amber/brown/sand/olive/panel/inset/header/line/chip/glass/card/track...); .dark body gets deeper blob gradients; dark clay (base + 6 tints) with black depth shadows + hue-retained dark gradients; dark neu/neu-inset/neu-pressed; dark al-add; dark scrollbars; dark shimmer. Mechanical remap of ~120 hard-coded hex utilities → var() tokens across all 14 agrilink components (ordered sed: hover → alpha overlays → plain); ACCENTS map now ships literal light+dark class pairs (dark tinted card heroes); freshness-ring track stroke via CSS var; special-case dark: variants (header border/shadow, error banners, follow pill, harvest chip).
- Feature B — REAL REVIEW SYSTEM: Prisma `Review` model (productId, buyerPhone?, name, rating 0.5-snapped, comment, lang, source BUYER|SEED, orderCode?); seed now writes 52 deterministic community reviews (dated in the past, EN/HI mixed); GET/POST /api/reviews (server-validated) + blended avg (catalog base ×8 + buyer reviews) + GET /api/products now includes reviewCount. UI: product sheet fetches live reviews, shows blended ★ + "{n} reviews", clay "Write a review" form (hover star picker, name prefilled from buyer, comment, POST → toast → cache invalidation); product cards show ★rating (count); DELIVERED order items get ⭐ Rate pills → open product sheet. docs/API.md + README + postman (2 new requests) updated.
- Feature C — PWA: public/manifest.webmanifest (standalone, theme #17a24f, bg #FFFBF2, 1024² icon + maskable), metadata manifest/appleWebApp/apple-icon.
- Feature D — Theme toggle: next-themes (attribute=class, system-aware) wrapped in Providers; neu circular Sun/Moon toggle in header with rotation micro-animation; hydration-safe mounted via useSyncExternalStore (lint clean).
- Bugs fixed: (1) ThemeToggle aria-label/title not mounted-guarded → Next dev hydration-attributes diff — guarded; (2) set-state-in-effect lint error in ThemeToggle → useSyncExternalStore noop-store pattern; (3) dev server restart needed for regenerated Prisma client (Review model) — killed + relaunched detached (setsid subshell), verified persistent.
- QA (agent-browser): light desktop E2E checkout PASS (AL-10006, ₹272/₹252 proof); dark desktop+mobile PASS (hero/grid/sheet/reviews/cart/AI/Hindi); review E2E PASS — 4★ form submit → toast "Thanks for reviewing!" → list shows new BUYER review → DB count 4/buyerCount 2/avg 4.9; Rate pills visible on delivered order (3); manifest 200; overflow 0 both themes; fresh-reload 0 page errors/0 hydration issues; ESLint clean; tsc src clean.
- Committed: round-12 commit (feat: dark mode + real reviews + PWA; fix: theme toggle hydration).

Stage Summary:
- AgriLink round 12: the app is now dual-theme premium (forest-night claymorphism is genuinely pretty, not an afterthought), reviews went from fake-by-construction to a real DB-backed loop (deliver → rate → blended rating), and the app is installable (PWA manifest).
- Key decisions: semantic --al-* token layer over raw hex (dark mode = token swap, not per-component dark: sprawl); blended rating keeps catalog trust while rewarding real buyers; BUYER-first review ordering surfaces fresh voices; ACCENTS carries literal dark classes so card heroes stay tinted in dark without JS.
- Known limits: reviews are per-product global (any buyer sees all — fine for pilot); seeded reviews are labeled SEED server-side but UI doesn't visually distinguish SEED vs BUYER-without-order (both get Community chip — only orderCode reviews get Verified buyer); theme default is light with system-awareness; single logo PNG reused for all manifest icon sizes.
- Next-phase suggestions: farmer "followers" DB counter + notify-on-fresh-harvest; AI picks personalization memory (last orders → recs); date-fns hi-IN relative times for reviews; export receipt as image share sheet; admin-lite stats view.

---
Task ID: 13 (cron review round 4)
Agent: Z.ai Code (main)
Task: webDevReview — assess state, agent-browser QA, fix bugs, add features (server-counted followers, Buy-again rail, receipt share + confetti), styling details, docs refresh.

Work Log:
- State check: worklog Task-12 reviewed; git clean at 2091864; health/stats healthy; lint + tsc clean. Baseline browser QA PASS (light/dark/mobile/checkout render, 0 console errors) → project stable → feature round.
- Feature A — SERVER-COUNTED FOLLOWERS: Farmer.followers Int @default(0) (db push, additive); seed gives each farmer a distinct audience (Dattatray 2318, Balvir 1543, Sunita 1284 …); new POST /api/farmers/follow {farmerId, follow} → atomic increment/decrement floored at 0, 404 on unknown farmer. Client Farmer type += followers; fmtFollowers() compacts 1284→"1.2k". Farmer sheet: pink ❤ followers chip next to rating, follow/unfollow now optimistic + API-synced with rollback+toast on failure, heart-pop micro-animation (new al-heart-pop keyframe). Verified live: browser follow 1284→1285 (server) + UI "1.3k", unfollow →1284; chip renders in light/dark/Hindi.
- Feature B — BUY-AGAIN RAIL (Blinkit-parity personalization): MarketView queries /api/orders?phone= for the persisted buyer, aggregates order items weighted by recency×qty, maps to live catalog, filters out-of-stock, renders a clay-green horizontal rail right under the hero ("🔁 Buy again — restock from the same farmers in one tap"). Verified: after placing an order the rail shows exactly that order's 3 products; empty for fresh visitors; light+Hindi "फिर से खरीदें" verified.
- Feature C — RECEIPT SHARE + CONFETTI: success step gains "Share receipt" (navigator.share when available → clipboard fallback → toast "Receipt copied 📋"; cancel silently ignored) with a plain-text receipt (order code, total, per-farmer payouts + refs, zero-middleman line). Deterministic 16-piece clay confetti (emoji + ₹ glyphs, CSS custom-prop driven al-confetti fall+spin) rains over the success receipt; both animations disabled under prefers-reduced-motion. Verified: toast appeared in headless browser.
- Bug fixed — STALE CART LINES: after a DB reseed product IDs change; persisted cart kept phantom lines (badge said 2, basket rendered empty). Added store.pruneCart(validIds) + a lint-safe deferred effect in MarketView that prunes lines whose product no longer exists → badge and basket can never disagree. Verified: badge auto-pruned 2→0 on reload after reseed.
- Styling details (mandatory): heart-pop on follow, confetti moment on payment success, clay-green buy-again rail, followers chip w/ compact k-formatting, i18n +9 keys EN/HI (followers/followErr/buyAgain/buyAgainSub/shareReceipt/receiptCopied).
- Docs: README feature table + API table (+follow row); docs/API.md POST /api/farmers/follow section; postman collection item 14 (follow, with tests); /api index now lists reviews + follow endpoints.
- Final QA: full E2E re-verified post-changes (3-item cart ₹298 → AL-10006 → Dattatray ₹220/Sunita ₹32/Balvir ₹24 proofs → share → track); mobile 390px overflow 0; fresh reload 0 page errors 0 console errors; ESLint clean; tsc src 0 errors.
- Committed: round-13 commit (feat: followers + buy-again + receipt share/confetti; fix: stale-cart prune).

Stage Summary:
- Round 13 closes the loop on trust + habit: farmers now carry real, server-counted audiences (social proof is data, not decoration), buyers get one-tap restock from their own order history (the #1 quick-commerce retention lever), and the payout-proof receipt became shareable — free marketing with every order.
- Key decisions: follow counter global vs per-user (counter global for honest proof, identity local in demo); recency×qty weighting over naive "last order" (frequent staples surface first); Web Share with clipboard fallback (mobile-first, desktop-safe); confetti deterministic (stable renders, reduced-motion safe).
- Known limits: buy-again needs the buyer phone persisted in store (i.e. ≥1 checkout on the device) — first-session visitors see the classic grid, by design; follows have no auth (demo); clipboard toast only verified via write path (headless blocks read-back).
- Next-phase suggestions: follow-driven "fresh harvest" toasts when a followed farmer restocks; per-farmer page w/ followers leaderboard; AI picks strip blended with buy-again signal; order scheduling ("weekly sabzi basket"); receipt image export.

---
Task ID: 14 (cron review round 5)
Agent: Z.ai Code (main)
Task: webDevReview — assess state, agent-browser QA, fix bugs, add features (delivery-slot scheduling, followed-farmers rail, promo-aware cart bar), styling details, docs refresh.

Work Log:
- State check: worklog Task-13 reviewed; git clean at cce5731; health/stats healthy (7 farmers, 22 products, ₹1,777 paid); lint + tsc src clean. Baseline browser QA PASS — full E2E (add×3 → FRESH10 −₹28 → pay AL-10007 → receipt with 3 payout refs → LIVE TRACKING + WhatsApp feed + countdown), dark mode orders view, AI basket "Plan a ₹300 veggie basket" → ₹180 basket + one-tap add-all, mobile 390px NO-OVERFLOW, 0 console/page errors → project stable → feature round.
- Feature A — DELIVERY SLOT SCHEDULING (Blinkit-parity): Order.slotKey added (schema additive db push; EXPRESS | TODAY_EVE | TODAY_NIGHT | TMR_MORN | TMR_EVE); POST /api/orders server-whitelists slotKey (unknown → EXPRESS fallback verified), scheduled slots set etaMin=240; DELIVERY_SLOTS + slotLabel() + PROMOS + promoDiscountFor() + finalBillFor() centralized in agrilink-client (single source for sheet, bar, orders UI); store.slotKey persisted; cart-sheet details step gets a clay-chip radiogroup (proper role=radio/aria-checked) with "Scheduled harvest" note when non-Express; receipt shows 🛵 slot chip; orders view shows 🕒 slot chip on card + "Scheduled · {window}" replaces truck ETA in StatusTimeline (live countdown chip kept). Server verified: AL-10008 (TMR_MORN persisted) + AL-10009 (HACKED_SLOT → EXPRESS).
- Feature B — FOLLOWED-FARMERS RAIL: MarketView computes followedProducts (farmerId ∈ followedFarmerIds, in-stock, freshness-sorted desc, top 8) → "🌱 From the farmers you follow" clay-berry rail under Buy-again; verified live: follow Dattatray → rail appears with his 4 products sorted 94→91→90→87 freshness; empty for users who follow nobody; dark + Hindi verified (आपके फ़ॉलो किए किसानों से).
- Feature C — PROMO-AWARE CART BAR (fixes round-10 known limit): promo state lifted to store (promoCode/setPromoCode; sheet Apply writes it, pay() consumes → null); CartBar now uses finalBillFor → shows discounted total + white "FRESH10 −₹25" chip; verified: sheet applies FRESH10 → bar reads "2 items · ₹247 [FRESH10 −₹25]" (was stale ₹272 pre-fix).
- Styling details (mandatory): new .al-clay-emoji medallion (puffy dual-shadow disc, light+dark) — hero floaties (🍅🥭🥬🥕🫑) now sit on clay circles instead of bare glyphs, big visual quality lift; empty states (cart 🥕, orders 📦, empty-category 🧺) get medallions too; empty-category card gains "Show everything" reset button; i18n +6 keys EN/HI (slotTitle, slotNote, fromFollowed, fromFollowedSub, showAll + slot labels live in DELIVERY_SLOTS).
- Browser E2E (agent-browser, incl. 390px mobile pass): slot radiogroup keyboard/ARIA semantics verified; scheduled order placed (AL-10010, receipt "🛵 Tomorrow · 7–10 AM", honest math: paid ₹247, farmer received ₹252); followed rail; promo bar; dark+Hindi all verified; fresh-reload 0 page errors, 0 console errors, NO-OVERFLOW desktop + mobile.
- Docs: docs/API.md POST /api/orders documents slotKey whitelist + fallback + etaMin semantics; postman collection order-create body adds slotKey (JSON-validated); README feature + API tables updated (followed rail, slot picker, slotKey row).
- Committed: 06638e9 (14 files, +222/−38).

Stage Summary:
- Round 14 closes three gaps vs Blinkit/Zepto at once: checkout now has delivery scheduling (Express vs windows), the social graph (follows) finally changes what the home page shows (freshness-sorted harvest feed from your farmers), and the floating cart bar stopped lying about the promo total — every surface now quotes the same finalBillFor.
- Key decisions: slot stored as server-whitelisted KEY, localized label resolved client-side (no locale data in DB); finalBillFor as the single billing truth shared by cart sheet + cart bar; scheduled etaMin=240 but UI leads with the slot label; promo consumed on successful pay.
- Known limits: scheduled slots don't gate the demo auto-advance clock (order still progresses every ~2.5 min — production would schedule the lifecycle); only 5 slots, no per-day capacity capping; followed rail is client-filtered (fine at 22 products, server filter at scale).
- Next-phase suggestions: gate auto-advance for scheduled orders (advance only inside the chosen window); WhatsApp-style "fresh harvest" toast when a followed farmer restocks; AI picks strip blended with buy-again + follow signals; receipt image export; per-farmer followers leaderboard.

---
Task ID: 15 (cron review round 6)
Agent: Z.ai Code (main)
Task: webDevReview — assess state, agent-browser QA, fix bugs, add features (weekly-basket subscription, slot-aware lifecycle gating, farmer leaderboard, receipt image export), styling details, docs refresh.

Work Log:
- State check: worklog Task-14 reviewed; git clean at 5e30efe; health 7 farmers/22 products; stats ₹2,469 paid, 89% share; lint + tsc src clean. Baseline browser QA PASS — full E2E (cart → FRESH10 −₹22 → slot persisted → pay AL-10011 → payout proof → LIVE TRACKING + countdown), dark/light toggle, AI budget chat ("cheap leafy greens under 50" → ₹42 basket + Add-all), WhatsApp feed toasts, mobile 390px overflow 0, 0 console/page errors → project stable → feature round.
- Feature A — SLOT-AWARE LIFECYCLE GATING (closes round-14 known limit): server SLOT_DEMO_OPEN_MIN offsets (EXPRESS 0, TODAY_EVE 4, TODAY_NIGHT 8, TMR_MORN 12, TMR_EVE 16 min — production-shaped logic over real window timestamps, compressed for demo); autoAdvance now uses windowOpensAt(order) as clock origin — before the window the order stays PAID; withCountdown returns awaitingWindow + nextStageInMin (window-relative when awaiting). UI: StatusTimeline shows "🕒 Scheduled · {window} · Harvest begins in N min" while gated, then normal live countdown. E2E PROOF: two same-age orders — EXPRESS advanced PAID→PACKED→OUT_FOR_DELIVERY→DELIVERED while TODAY_EVE stayed PAID with countdown; after its window opened awaitingWindow flipped false and the clock started.
- Feature B — WEEKLY-BASKET SUBSCRIPTION (Blinkit-parity retention): Prisma Order.subscription (null|'WEEKLY') + Order.nextDeliveryAt (+7 days, additive db push); POST /api/orders whitelists subscription (unknown → 400 "try WEEKLY" verified), WEEKLY = 5% of items cap ₹150, PLATFORM-SUBSIDIZED and STACKS with promos (farmer payout still 100% of items); SUBSCRIPTIONS/subDiscountFor/finalBillFor(3-arg) in agrilink-client (single billing truth); store.subscribed persisted; cart-sheet details step gets clay-green checkbox toggle ("🔁 Make it a weekly basket −5%", animated switch, proper role=checkbox/aria-checked); bill lines + receipt lines + "🔁 Weekly basket subscribed · Next basket {date}" chip; CartBar discount chip now covers promo-or-sub (₹62 = 32+29+3−2 verified live); OrdersView "🔁 Weekly · next 27 Sept 2026" chip. Server verified: AL-10012 (₹220 item → farmer ₹220 untouched, total 267−11=256, nextDelivery +7d).
- Feature C — FARMER LEADERBOARD rail ("🏆 Most loved farmers"): client-side dedupe of farmer-embedded products (7 over 22), top 4 by REAL server-counted followers; clay-mango rail with 🥇🥈🥉🏅 medal chips, clay-emoji avatar, village+state+rating, followers (k-compact) + product count, "⚡ ₹X earned directly" proof, inline follow/unfollow (optimistic store + server sync via existing follow API); card click/Enter/Space → farmer sheet. Verified light+dark+Hindi+mobile.
- Feature D — RECEIPT IMAGE EXPORT: canvas 2D-rendered PNG (no deps) — green header w/ brand + order code/date, "Paid via UPI ₹X", per-farmer payout rows (avatar, name+₹, upiId+payoutRef, ✓), promo/sub discount lines, trust footer; Save-as-image button beside Share receipt → toBlob → download `AgriLink-AL-100XX.png` + toast (silent on headless block). Verified: file downloaded (56KB), Hindi rendering perfect; fixed initial dead-space by tightening height formula (320→270 base).
- Styling details (mandatory): leaderboard medal ring + hover-lift cards; subscription toggle switch w/ al-pop knob + clay-green active; weekly chips with green ring in orders; i18n +11 keys EN/HI (subToggle/subToggleSub/subLine/subReceipt/subNext/saveImage/imageSaved/topFarmers/topFarmersSub + harvest-begins inline); receipt canvas localized EN/HI.
- QA (agent-browser): E2E with subscription + scheduled slot (AL-10015: TODAY_EVE + WEEKLY stacked −₹2, receipt chip + orders chip + gating countdown all verified); receipt PNG EN+HI; leaderboard follow toggle; dark+Hindi market/orders; mobile 390px overflow 0; fresh-reload 0 page errors, 0 console errors; ESLint clean; tsc src 0 errors.
- Dev-server note: pkill+setsid relaunch died when the tool shell exited; stable method = `(bun run dev > /tmp/al-dev.log 2>&1 &)` subshell — use this next time.
- Docs: README trust-math (− subscription), feature rows (leaderboard, subscription toggle, save-image, gating), API table (awaitingWindow, subscription param); docs/API.md GET/POST orders (gating offsets, WEEKLY whitelist, response shape + subDiscountRs); postman item 7 body now includes slotKey+subscription with updated description.
- Committed: e05ad45 (14 files, +395/−50).

Stage Summary:
- Round 15 closes the two biggest behavioral gaps (scheduled orders now honestly wait for their window; recurring revenue via weekly baskets) and adds the two biggest shareability/social wins (farmer leaderboard as living social proof; the payout-proof receipt is now an image you can post anywhere).
- Key decisions: window gating lives server-side (client only renders awaitingWindow) — production can switch the offsets for real timestamps with zero UI change; subscription stacks with promos but both are platform-funded so the farmer story never bends; leaderboard derived client-side from farmer-embedded products (no new endpoint at 22 products).
- Known limits: subscription has no billing/cancellation backend (demo toggle; next order just isn't discounted unless toggled on); scheduled gating uses fixed minute offsets, not calendar windows; leaderboard shows global followers (no per-user "you follow" rank); receipt PNG uses system fonts (emoji ✓ rendered by browser).
- Next-phase suggestions: cancel/skip UI for the weekly basket ("Manage subscription" in orders view); AI picks blended with buy-again + follow signals (personalization memory); per-farmer mini-page with product grid + follower milestone celebrations; order-scheduling capacity capping per slot; receipt PNG QR code linking to the order's public proof page.

---
Task ID: 16 (full audit + backend completion round)
Agent: Z.ai Code (main)
Task: Full application audit → feature gap research → complete backend (cancellation/wallet/notifications/addresses/coupons/referrals/subscription/issue/tip) → frontend wiring → E2E verification → docs → commit everything.

Work Log:
- AUDIT: worklog rounds 1-15 reviewed; dev.log clean; health 7 farmers/22 products; lint + tsc (src) clean; git content-clean (204 filemode-only diffs normalized via core.filemode=false). Browser baseline PASS. Identified 9 critical gaps vs Blinkit/Zepto parity + honest-commerce: no order cancellation/refunds, no wallet, no notification center, unmanageable subscription, single coupon, no saved addresses, no delivery tip, no referral program, no issue reporting.
- SCHEMA (+3 models, +12 Order fields, additive db push): Order += tipRs, walletUsedRs, cancelReason, cancelledAt, refundRs, refundRef, issueReason, issueStatus (status now also CANCELLED); WalletTx (phone, type WELCOME|REFERRAL|REFUND|SPEND, signed amountRs, note, ref); Notification (phone, kind ORDER|WALLET|SOCIAL|SYSTEM, icon, title, body, readAt); Address (phone, label Home|Work|Other, receiver, line, city, isDefault).
- BACKEND (7 new route files + 2 upgraded):
  * POST /api/orders/[code]: CANCEL (PAID/PACKED only → per-farmer mock Razorpay-X payout reversals rev_*, farmer earnings adjusted, stock restored, full refund → WalletTx REFUND + refundRs/refundRef/cancelReason/cancelledAt + wallet notification; 409 on OUT_FOR_DELIVERY/double-cancel) | ISSUE (DELIVERED only → issueReason + OPEN + support notification).
  * GET /api/coupons (server renders the same PROMOS table the UI chips do).
  * GET/POST/DELETE /api/addresses (label whitelist, first-address auto-default, delete promotes most recent, ownership-checked).
  * GET /api/wallet (Σ signed ledger + last 25 txs).
  * GET/POST /api/notifications (latest 20 + unread; mark read all or ids[]).
  * GET/POST /api/referrals (deterministic per-phone AGR-XXXXXX code; apply resolves inviter by deriving codes for all known buyer phones; guards: own-code, double-claim, validity; ₹50 credit + notification).
  * GET/POST /api/subscription (SKIP +7d / CANCEL clear / RESUME re-arm; 404/409 guards; notifications).
  * POST /api/orders upgraded: tipRs whitelist 0/10/20/30/50 (100% rider pass-through), useWallet (min(balance, bill) → walletUsedRs + negative SPEND tx), coupon book with min-order 400s, order-confirmed notification; PROMOS/SUBSCRIPTIONS now imported from agrilink-client (single source of truth); autoAdvance/withCountdown hardened against CANCELLED/unknown statuses (bug found in audit: CANCELLED orders would have been force-mutated back to PAID by indexOf −1).
- CLIENT LIB: PROMOS → { pct|flat, cap, min, en, hi } + couponList()/couponErrorFor(); TIP_OPTIONS; referralCodeFor(); billFor/finalBillFor +tipRs param; Order/AddressT/WalletTxT/NotificationT/WalletResponse/NotificationsResponse/CouponT/ReferralResponse types; apiDelete().
- SEED: welcome ₹25 + referral ₹50 + refund ₹53 wallet txs; Home/Work addresses; 4 notifications (1 read); 6th demo order CANCELLED (refund proof + reason) — farmer earnings honestly not incremented for it.
- UI: NEW notifications-bell.tsx (clay panel, unread badge, ago-times, auto mark-read 600ms after open, 25s poll) in header. OrdersView rebuilt: "Your money" card (wallet balance + expandable signed ledger + referral code + copy + apply-friend-code input), CancelBox (inline confirm with reason), IssueBox (textarea → OPEN chip), SubManage (expand SKIP/STOP + RESUME when inactive), cancelled banner (reason + refund chip + ref), tip/wallet chips on cards, CANCELLED pill + timeline hidden. CartSheet upgraded: coupon chips row (active clay-green / locked dimmed with "add ₹X more"), saved-address picker (revealed LIVE as phone is typed — form-driven lookupPhone), save-this-address checkbox (persists pre-pay, never blocks payment), tip radio group, wallet toggle (balance + live walletApplied), grand-total everywhere, receipt (HTML + canvas PNG) gained tip + wallet lines. Header: bell between theme toggle and cart.
- i18n: +72 EN/HI keys across notifications/wallet/tip/coupons/addresses/cancel/issue/subscription/referral.
- API E2E (curl): order with MANGO20+tip20+wallet128 → ₹115 total, farmer ₹220 untouched ✓; cancel → refund 115, rev ref, wallet 115, stock restored, double-cancel 409 ✓; subscription SKIP/CANCEL/RESUME ✓; ISSUE → OPEN ✓; mark-read 6→0 ✓; referral apply +₹50, double-claim/own-code/bogus-code guards ✓; SABZI50 min gating in couponErrorFor ✓.
- BROWSER E2E (agent-browser): full checkout (3 items → coupon chip MANGO20 −₹55 → details → saved addresses appeared live on typing Anjali's phone → Home picked → tip ₹20 → wallet ON (−₹128) → pay ₹135 receipt showing MANGO20/tip/wallet lines + "₹128 from wallet" toast → bell badge 4); money card + ledger (5 honest entries); cancel E2E (confirm box → reason → "Order cancelled — ₹135 is in your wallet ⚡" toast, badge 5, cancelled banner, wallet ₹135); notification panel (5 items incl. live refund at top, mark-read cleared badge); issue report ("Coriander arrived slightly wilted" → Issue reported chip); subscription E2E as Sneha (weekly toggle → ₹47 order → Manage → SKIP → toast + date pushed); Hindi (ऑर्डर रद्द करें/साप्ताहिक टोकरी मैनेज करें/AGRILINK वॉलेट) + dark mode + mobile 390px overflow 0; fresh reload 0 console errors; dev.log clean.
- DOCS: README (feature table +9 rows, API table +7 endpoints, architecture WalletTx/Notification/Address + reversals), docs/API.md (orders POST tip/wallet/coupons pipeline, new sections for orders/[code], coupons, addresses, wallet, notifications, referrals, subscription), postman +13 requests (28 total, JSON-validated), /api index 18 endpoints.
- Committed: round-16 commit (feat: money & account completeness — cancel+refund, wallet, notification center, coupons, addresses, tip, subscription manage, referrals, issue reporting).

Stage Summary:
- Round 16 closes every money-and-account gap: the platform now behaves like a real quick-commerce app after the payment too — orders can be cancelled with an HONEST money trail (per-farmer payout reversals + instant wallet refund), refunds/referrals land in a spendable wallet, the bell makes server-persisted history visible, checkout has addresses/tips/coupons like Blinkit, and the weekly basket is fully manageable.
- Key decisions: refund destination is the wallet (honest: UPI reverse-collect takes days; wallet is instant + spendable); cancel allowed only before OUT_FOR_DELIVERY; PROMOS as the single client/server coupon truth; wallet holds buyer-side money only (farmer payouts never pass through); referral codes derived from phone (no extra table, works cross-device); addresses revealed live from the form phone (no store-write needed).
- Known limits: refund = totalRs incl. tip (tip refund is generous-but-simple); reversal refs returned at cancel time are not persisted per-order (only refundRef is — reversals visible in toast/history only); referral inviter resolution scans distinct order phones (fine at demo scale, needs a Referral table in prod); notification auto-read is all-at-once (no per-item read tracking); subscription SKIP applies to latest sub order for the phone.
- Next-phase suggestions: per-item notification read states; Tip "after delivery" flow (charge tip on delivery instead of checkout); capacity capping per delivery slot; receipt PNG QR → public proof page; AI picks blended with wallet+buy-again+follow signals; admin-lite stats view; Segmented delivery-partner entity so tips have a named receiver.

---
Task ID: 17 (GitHub publish round)
Agent: Z.ai Code (main)
Task: Push the entire codebase to github.com/arpitnayan123-bot/Agri-link, make README.md premium (animations, screenshots, demo video), publish a live landing link on top.

Work Log:
- ASSETS: captured 12 retina screenshots via agent-browser (market light/dark, catalog, farmer sheet, cart + payout preview, coupon, checkout w/ slots+tip, instant-payout receipt w/ confetti, live tracking, orders+wallet, mobile 390px); recorded full 90s demo flow at 30fps w/ cursor (browse → add → cart → MANGO20 → details → UPI pay → receipt → track); ffmpeg sandbox OOM-workaround = chunked seeked encodes (12×15s @2x) → concat → 3.3MB MP4; palettegen GIF 640px/22s/4.3MB; screenshots optimized 2880→1600px.
- CLEAN REPO: created orphan branch `agri-main` with ONLY project files (137 files: src, prisma, public, docs, postman, mini-services, configs, README, worklog) — excluded sandbox cruft (skills/ 68M, tool-results/ 22M, examples, tests, db binary, .env); rewrote .gitignore for the clean tree; single root commit 0137f0e "feat: AgriLink — farmer-direct quick-commerce platform"; pushed to GitHub main (replaced placeholder initial commit via --force-with-lease).
- PREMIUM README: capsule-render waving gradient banner (brand greens 14532d→16a34a→84cc16) + footer wave; LIVE DEMO link first thing on top; 3 for-the-badge CTAs; readme-typing-svg tagline rotator; tech-stack badges; demo GIF inline (auto-loop) + MP4 link; screenshot gallery (4×2 grid + collapsible "more" with mobile/coupons/how-it-works); trust-math block; condensed feature tables (money / market / account); full API table (collapsed); architecture ascii; honest limitations; docs links. Fixed purple→green gradient via plumbing commit e38494a (sandbox checkpoint process races branch switches — switched to plumbing: hash-object → temp index → write-tree → commit-tree → update-ref).
- PUBLISHED LINK: built premium gh-pages landing (self-contained index.html): animated gradient blobs, floaty veg emojis, count-up stats, scroll reveals, shine-gradient headline, clay cards, dark trust-math panel, 10-shot gallery, quickstart block; assets committed to gh-pages branch (15M incl. GIF/MP4); enabled Pages via API (source gh-pages root); site LIVE at https://arpitnayan123-bot.github.io/Agri-link/ (HTTP 200, browser-verified: hero, GIF frame, stats, trust math, gallery all render).
- REPO METADATA: description + homepage (the Pages URL) + 16 topics set via API (nextjs, quick-commerce, agritech, fintech, upi, razorpay, claymorphism, pwa …).
- VERIFIED: repo tree clean (137 files, TypeScript 95.4%), README renders (banner, badges, typing SVG, gallery), Pages 200 with full landing, local docs restored from agri-main after checkpoint race.

Stage Summary:
- The repo is now a portfolio-grade artifact: clean single-commit code history, premium animated README with the published link on top, a real live landing page, and a 90-second demo video + looping GIF.
- Key decisions: orphan-branch snapshot pushes (clean curated tree per push, immune to sandbox checkpoint races; future rounds: edit → plumbing/snapshot commit → push agri-main:main); gh-pages hosts the landing (same-origin assets); GIF inline for autoplay, MP4 for full quality.
- Known limits: sandbox OOMs make long single-pass ffmpeg runs die — use chunked seeked encodes; the sandbox background git process flips branches/untracked files mid-work — never trust working-tree git state for pushes, use plumbing or re-restore from the pushed branch; PAT stored in remote URL gets auto-redacted in .git/config after a while (re-set before pushing).
- Next-phase suggestions: GitHub Actions CI (lint + tsc on push); add LICENSE + repo social preview image; Open Graph image route in the app; release tags (v0.1.0-pilot); auto-push snapshot cron so GitHub tracks every dev round.

---
Task ID: 18 (Live app link swap + repo republish)
Agent: Z.ai Code (main)
Task: Replace the GitHub Pages landing link with the REAL deployed app URL (https://f160t8hueub0-d.space-z.ai) at the top of the GitHub repo README, re-push the full clean codebase.

Work Log:
- VERIFIED deployment live: https://f160t8hueub0-d.space-z.ai → HTTP 200, /api/health → {"status":"healthy","farmers":7,"products":22}.
- README.md: top CTA is now "🟢 ▶ OPEN THE LIVE APP: https://f160t8hueub0-d.space-z.ai" with (a) for-the-badge OPEN LIVE APP shield (googlechrome logo, brand greens), (b) shields.io LIVE uptime badge that real-time pings the deployment (ONLINE/DOWN), (c) Pages landing demoted to secondary "🪺 Project Landing" badge; "Run it locally" section gained a no-setup callout linking the live deployment with a MANGO20 demo path; footer CTA updated to the live app URL with the host shown on the shield.
- SAFETY: local main tracks 1472 files incl. .env and sandbox cruft (skills/ tool-results/ examples/ tests/) — confirmed it must never be pushed directly; clean-snapshot publish pattern (round 17) reused.
- PUBLISH (plumbing, no branch switch — avoids the known sandbox checkpoint race): GIT_INDEX_FILE temp index → read-tree origin/main (137-file curated tree) → hash-object updated README.md + worklog.md → update-index cacheinfo → write-tree → commit-tree (orphan, no parent) → push <sha>:refs/heads/main --force-with-lease. Local main history left intact.
- REPO METADATA: PATCH /repos/arpitnayan123-bot/Agri-link homepage → https://f160t8hueub0-d.space-z.ai (description/topics unchanged).
- VERIFIED: raw.githubusercontent README shows the space-z link at top; shields uptime badge renders ONLINE; repo homepage metadata updated; repo page contains the live app link above the fold.

Stage Summary:
- The GitHub repo now opens with the REAL deployed app link (https://f160t8hueub0-d.space-z.ai) — not the static Pages landing — plus a self-updating uptime badge; the Pages landing remains as a secondary showcase.
- Key decisions: keep the orphan-snapshot publish pattern (clean curated 137-file tree per push); uptime via shields.io website badge (zero infra).
- Known limits: sandbox deployment URL (f160t8hueub0-d.space-z.ai) is ephemeral infra — if the sandbox restarts the badge will show DOWN; the gh-pages landing CTAs still point at Pages content (cosmetic only).
- Next-phase suggestions: add LICENSE + social preview image; GitHub Actions CI (lint+tsc); release tag v0.1.0-pilot; OIDC-guard the PAT (rotate immediately — it was exposed in chat).
